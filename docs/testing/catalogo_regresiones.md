# Catálogo de regresiones prioritarias

| ID | Origen | Riesgo | Regresión requerida | Estado |
|---|---|---|---|---|
| QA-REG-001 | TK-2026-07-28-003 | Crítico | Un error sintáctico, de importación o ejecución debe dejar editor, barra, canvas, LCD y telemetría en `ERROR`; la siguiente ejecución o reset debe ser posible. | Cubierto por `test_worker_error_event_transitions_ui_to_terminal_error_state` y `test_desktop_session_adapter_converts_terminal_worker_error_to_terminal_state` |
| QA-REG-002 | TK-2026-07-28-002 | Medio | La matriz de conformidad debe declarar `Screen.draw_text` como soportado o no soportado; si se implementa, LCD y prueba UI deben mostrarlo. | Pendiente de decisión de producto |
| QA-REG-003 | WEB-F-001 | Alto | Tras `finished`, editor, canvas, LCD y telemetría deben consumir el mismo snapshot terminal. | Pendiente de revalidación |
| QA-REG-004 | WEB-F-002 | Alto | Detener/reiniciar restaura mundo activo, robot, tick, tiempo, LCD y telemetría sin duplicados. | Pendiente de revalidación |
| QA-REG-005 | WEB-F-003 | Alto | En 390×844 el canvas se adapta al contenedor y ningún control queda recortado. | Pendiente de revalidación |
| QA-REG-006 | WEB-V-017 | Alto | Cancelar o reiniciar una misión no puede conservar ni presentar su resultado como “Misión completada”; debe limpiar o identificar explícitamente la cancelación. | **Corregido y cubierto**: el reinicio limpia el resultado y descarta eventos de misión atrasados por generación de snapshot. La prueba Playwright `test_reset_hides_the_terminal_mission_result` aprobó el 2026-07-30. |
| QA-REG-007 | WEB-V-018 | Alto | Al pausar, editor, barra de estado, controles y resumen de telemetría deben presentar el mismo estado `paused`; no puede conservarse `running` hasta la reanudación. | **Corregido y cubierto**: pausa y reanudación publican inmediatamente un snapshot decorado con el estado de sesión. La prueba Playwright `test_simulation_controls_follow_execution_state` verifica `sessionStatus` y `telemetryStatus` en pausa (2026-07-30). |
| QA-REG-008 | WEB-V-026, WEB-V-034 | Media | Crear o cargar un mundo vacío sin una acción explícita de colocar no debe intentar colocar un asset ni emitir un error de colocación. | **Corregido y cubierto**: crear, redimensionar e importar ya no persisten implícitamente un robot. `test_world_editor_blank_canvas_does_not_attempt_an_unselected_placement` aprobó en Playwright el 2026-07-30. |
| QA-REG-009 | WEB-V-033 | Alta | Una sesión iniciada con Depurar y breakpoint debe pausarse o finalizar de acuerdo con el script. “Detener y reiniciar” debe cancelar siempre la sesión de depuración y restaurar controles, tick, tiempo, telemetría y estado `created`. | **Corregido y cubierto**: el evento de breakpoint transiciona la sesión a `paused`, publica el snapshot coherente y `debug_continue`/`debug_step` la restauran a `running`. `test_reset_recovers_a_session_paused_at_a_debug_breakpoint` aprobó en Playwright el 2026-07-30. |
| QA-REG-010 | WEB-V-036 | Alta | Detener y reiniciar debe completar siempre el reinicio de una misión o escenario normal: cancelar el worker, restaurar estado `created`, pose, tick, tiempo, telemetría, LCD y controles en un plazo acotado. | **Revalidada y cubierta**: `test_reset_recovers_the_ultrasonic_obstacle_scenario` ejecuta “Ultrasonido + obstáculos” y verifica retorno a `created`, telemetría coherente y controles recuperados (Playwright, 2026-07-30). |
| QA-REG-011 | WEB-V-042 | Media | Ayuda → Manual de uso debe abrir el manual en la misma pestaña, una pestaña nueva o un diálogo accesible; si no está disponible debe informar claramente al usuario. | **Revalidada y cubierta**: el enlace abre `/help` en una pestaña nueva con contenido de ayuda; `test_help_menu_opens_the_user_manual` aprobó en Playwright el 2026-07-30. |
| QA-REG-012 | E2E-ACC-001 | Media | Todo texto crítico debe alcanzar contraste WCAG AA de 4.5:1 en tema claro y oscuro. | **Corregido y cubierto**: la paleta oscura aplica color contrastado a los valores del resumen. Los 10 pares críticos medidos aprobaron por Playwright el 2026-07-30. |
| QA-REG-013 | RELEASE-STATIC-001 | Alta | La construcción Windows debe conservar y usar `SimuladorEV3.spec`, que declara assets y DLL de Tcl/Tk. | **Corregido estáticamente y cubierto**: el script conserva y pasa la especificación absoluta a PyInstaller. Falta validar el ejecutable resultante en un Windows limpio. |

Cada ítem deberá enlazarse desde el informe de campaña con el caso de prueba,
la prueba automatizada, comando y evidencia visual cuando corresponda.
